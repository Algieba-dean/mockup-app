## 1. Audit existing specs

- [x] 1.1 列出 `openspec/specs/` 下所有与商店截图美化相关的 capability，标记 Purpose 为 TBD 的条目

## 2. Fill missing Purpose sections

- [x] 2.1 补全 `screenshot-canvas` 的 Purpose
- [x] 2.2 补全 `device-mix` 的 Purpose
- [x] 2.3 补全 `multi-size-export` 的 Purpose
- [x] 2.4 补全 `panoramic-background` 的 Purpose
- [x] 2.5 补全 `skew-and-floating` 的 Purpose
- [x] 2.6 补全 `advanced-templates` 的 Purpose（含与 `customization-controls` 的交叉引用）
- [x] 2.7 补全 `customization-controls` 的 Purpose（含与 `advanced-templates` 的交叉引用）

## 3. Build the master doc system

- [x] 3.1 创建 `openspec/specs/README.md`，按流水线阶段索引全部 10 个 capability
- [x] 3.2 为每个 capability 条目附一句话摘要 + 相对路径链接
- [x] 3.3 校对交叉引用链接的正确性（无死链接）
